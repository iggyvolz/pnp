#!/usr/bin/env php
<?php

use iggyvolz\Pnp\CLI;

foreach ([__DIR__.'/../vendor/autoload.php',__DIR__.'/../../../autoload.php'] as $path) {
    if (file_exists($path)) {
        require_once $path;
        break;
    }
}
CLI::go();